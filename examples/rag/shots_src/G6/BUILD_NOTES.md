# G6 构建笔记（第 3 章下半：SC26–SC30，解说 S26–S30，帧 4677–5696）

预览合成 `RagG6`；`SHOTS_G6` 5 个镜头首尾相接，`BG_G6 = []`（无背景覆写）。所有动画为 N 的纯函数（`N = useCurrentFrame() + F0`），随机只用 `rnd`。

## 镜头表
| id | 帧 | 文件 | 节拍 → 元素 | 内容 |
|---|---|---|---|---|
| SC26 | 4677–4926 | `SC26.tsx` | 4685 `Rerank` TechText glitch / 4707 「召回 25 条」+ 25 张 MiniCard 升起淡入（0.6 帧错峰）/ 4734 紫色 `TrapRag`「Cross-Encoder 重排」slideUp Δ260 / 4823 配对卡「差旅报销上限？+段落」自左滑入 → 4838/4842 双箭头 draw-on → 4846 相关分框 glitch + 0.00→0.92 计数（18 帧）/ 4890 25 卡穿过梯形：22 张变灰加速下落（y≥600 前淡尽）、3 张（i=5/12/19）easeInOut 落到底部放大变紫 / 4914 「Top 3」 | 末 6 帧线性淡出后硬切 SC27 |
| SC27 | 4927–5017 | `SC27.tsx` | 4931 起两组三层迷你漏斗 slideUp Δ160（层间 2 帧、右组晚 2 帧）：左「向量召回/混合检索/重排」右「召回/粗排/精排」灰灰紫 / 4935·4937 标题 Pill「RAG 检索」「推荐系统」glitch / 4946 「≈」glitch / 5001 橙 TagBlock「贵」21 帧缩放 / 5006 紫 TagBlock「准」+ 5008 绿勾 draw-on | 末 2 帧淡出（1−n/3） |
| SC28 | 5018–5313 | `SC28.tsx` | 5022 「上下文」竖条（160×400，8 段，2 段紫色关键证据在中间）上浮 Δ40 + 淡入 / 5060 曲线卡 440×300 缩放入场 / 5077 U 形曲线 clipPath 按 x 揭示 24 帧 + 中段 4 段变暗 12 帧 / 5089 中点红点 + 5091 「中间最差」/ 5154 「Lost in the Middle」Exo 2 紫 + 5158 「Liu et al., TACL 2024」/ 5244 两段紫证据 18 帧移到条顶/条底（其余段顺移），5262 起脉冲 1.08 + 紫柔光 | 末 6 帧线性淡出 |
| SC29 | 5314–5564 | `SC29.tsx` | 5318 Prompt 卡（问句 Pill + 证据 [1][2][3] + 分割线）上浮 Δ40 / 5322 LLMIcon「大模型」21 帧缩放 / 5340 箭头 → 5342 问句 + 3 张证据副本 3 帧错峰飞入大模型（24 帧，缩至 0.28 并淡出）→ 5368 LLM 脉冲 1.12 / 5396 箭头 → 5401 答复卡 slideUp Δ120 → 5416 起打字机 2 帧/字 / 规则 ①②③ 打字机 5407/5472/5517 / 5517 角标 [1][2] glitch（占位不回流）/ 5551–5564 Prompt 卡 + LLM + 箭头 加速下摇 0.9n² + 13 帧淡出，**答复卡不离场** | 组界交给 SC30 |
| SC30 | 5565–5696 | `SC30.tsx` | 5565–5579 答复卡自 (740,310) 移到 (430,192) / 5575·5577 角标 [1][2] 紫折线 draw-on 14 帧到来源卡（右「员工手册 p.12」← [1]，左「差旅补充规定 2025」← [2]，折线不交叉）/ 5583·5585 来源卡缩放入场，5589·5591 起紫边柔光 / 5631 Pill「可追溯 · 可核查」glitch / 5633 「RAG 的价值」46px/900 GlitchIn rgbSplit 4 | 末 2 帧淡出（进章节卡） |

## 本组图元（`src/rag/shots/G6/ui.tsx`）
- `TrapRag`：复制 R2 `Trap`，改为显式 `id`（不用 Math.random）+ `children`（可混排 `TrapTech` Exo 2 白斜体 + `TrapText` Noto）；`STOPS/mixStops/GREY_STOPS/PURP_STOPS` 沿用 R2 SC014 非对称水平渐变。
- `MiniCard`（召回候选小卡：grey/active 连续混色）、`EvidenceCard`（[n] 标签证据卡）、`SourceCard`（来源卡，active 紫边+柔光）、`AnswerCard`（答复卡 420×180，SC29/SC30 共用；`typed` 打字机、`citeShow/citeOp` 角标占位与透明度分离）、`DrawPath`（折线 draw-on）、`TypeLine/typedCount`（打字机行）、`mixHex`（支持 `#hex` 与 `rgb()` 嵌套混色）。
- 复用 `../../ui`：CText/TechText/Box/Pill/TagBlock/Svg/LineArrow/Check/LLMIcon + fadeIn/slideUp/scaleIn/exitAccel；`../../common`：GlitchIn/kf/easeInOutPow/emphasisPulse/powOutRemain/rnd/glitchOpacity/FONT_*。

## 关键参数 / 决策
- **示例文本**：问句「差旅报销的上限是多少？」（SC29）/「差旅报销上限？」（SC26 配对卡短版）；答复「差旅住宿上限为每天 600 元[1]，一线城市 800 元[2]。」；来源「员工手册 p.12」「差旅补充规定 2025」。
- **数字出处**：召回 25 条 → Top 3 = Pinecone 示例（调研 §3.2、§7.1 #11）；相关分 0.92 为分镜表给定示意值；曲线卡横轴 1→20（Lost in the Middle 20 文档设定，调研 §3.4），纵轴只标「准确率」不标数值（避免未核实数字）；`Cross-Encoder`/`Rerank` 拼写见调研 §3.3。
- 分镜表写「12 张小卡标 30 条」，改为 **25 张小卡 + 「召回 25 条」**，使数字与调研有出处一致（30 条无出处）。**主会话已裁定保留。**
- 「Liu et al., TACL 2024」按分镜表「全局约束」；调研正文 §3.4 写 "TACL 2023"（论文 arXiv 2023-07，TACL vol.12 于 2024 年刊出）——**主会话已裁定：保留 TACL 2024（正式刊出年）**。
- 安全区：全部内容 y≥175（rail y112–160 由 G0 绘制，still 中已出现并无重叠）、y≤620；SC30 rail 已于 5564 消失，仍守 y≥175（答复卡「回答」tab 顶 176）。
- 入场位移：SC28 上下文条（高 400）与 SC29 Prompt 卡（高 410）如用 Δ300 会穿过字幕带，改为 Δ40 上浮 + 10 帧淡入；SC27 漏斗 Δ160、SC29 答复卡 Δ120，均不进 y≥637。
- SC26 淘汰卡下落：`y = 205 + 495·u²`（20 帧，0.25 帧错峰），`opacity = 1 − clamp((y−520)/80)`，y≥600 消失，不触字幕带/进度条。

## 自检（`rag/stills/G6/`，共 50 张）
- SC26：4677(组界首帧) 4690 4712 4715 4740 4760 4835 4870 4900 4910 4918 4926 — 发现并修复：`mixHex` 嵌套解析 rgb() → NaN 使 25 卡不可见；卡片自上落下穿 rail/压标签 → 改自下升起；尾卡拖到 Top 3 之后 → 错峰 0.4→0.25、时长 22→20。
- SC27：4928 4940 4955 4990 5010 5017 — 修复：漏斗 Δ300 穿字幕带 → Δ160。
- SC28：5019 5030 5050 5070 5090 5160 5250 5262 5290 5313 — 修复：上下文条 Δ300 → Δ40。
- SC29：5315 5330 5350 5362 5375 5410 5440 5480 5520 5540 5558 5564 — 通过（角标 glitch 期间占位不回流；末帧只剩答复卡）。
- SC30：5565 5572 5580 5590 5600 5640 5660 5696(组界末帧) — 通过（5565 与 5564 答复卡同位；引线锚点与角标对齐，估算值无需微调）。
- `boundary_first_4677.png` / `boundary_last_5696.png`。
- 组间空白帧：SC26→SC27 4927–4930（4 帧仅星空）、SC27→SC28 5018–5021（4 帧）、SC28→SC29 5314–5317（4 帧）；SC29→SC30 无空白。

## tsc / 测渲
- `npx tsc --noEmit`：最终全工程 **exit 0**（过程中曾见 G7 `SC35.tsx` 缺 `./fonts` 的报错，非本组文件，已由 G7 自行修复）。
- 30 帧测渲（jpeg 序列，输出 /tmp 后已删）：空载时 SC26 落卡段 4894–4923 **3.1 s**、SC28 移位段 5243–5272 **2.9 s**、SC29 飞入段 5341–5370 **2.5 s**（≈9.7–12 fps）；磁盘清理后用精简 bundle `build_dev_g6`（117 MB）在系统 load ≈36（多组并发渲染）下复测：首跑冷启动 11.7 s，随后 **7.4 s / 5.8 s**（≈4–5 fps），仍在协议 4–12 s 区间；无 blur/滤镜，单帧 DOM ≤ ~200。
- still 规则（主会话 18:57 更新）：只用 `still.sh`，tag 固定 `g6`，改代码先 `rm -rf remotion/build_dev_g6`；清盘后用新 bundle 复出 4918/5160/5660 三帧，与此前一致。

## 未完成 / 降级项
- 无未完成镜头。降级：SC26 分镜表「12 张卡 / 30 条」→ 25 张 / 25 条（见上）；SC28 曲线纵轴无刻度数字（有意）。
- SC29 角标 sup 用 Orbitron 20px（正文 26px 的上标），低于 22px 最小字号规则——**主会话已裁定放行（上标属例外）**。

## 对共用层 / 主会话的建议（裁定状态）
1. ~~定稿 Lost in the Middle 引用年份~~ → 已裁定 TACL 2024，无改动。
2. `mixHex`（hex/rgb 双解析）、`DrawPath`（折线 draw-on）、`TypeLine`（打字机）通用性较高，可考虑合入 `src/rag/ui.tsx` → **已裁定先留在组内**。
3. still.sh 在 zsh 下末尾会打印一条 `command not found: --out-dir/TMPDIR` 噪音（脚本第 14–15 行），不影响输出。

## 状态
- 四项待决事项已由主会话裁定（TACL 2024 / 25→3 / 20px 上标放行 / 通用件留组内），**G6 无需改动，等待成片 QC**。

## QC v1 修复（rag/qc/qc_v1_C3.md · G6 条目，2026-09-06）
still：`rag/stills/F56/`（tag `g56`，与 G5 共用一个 bundle）；`npx tsc --noEmit` 通过；30 帧测渲 SC26 落卡段 4893–4922 **2.6 s**（≈11 fps）。
| QC 条目 | 严重度 | 处理 | 复核 still |
|---|---|---|---|
| SC25→SC26 组界 / 4685–4706 近空画面 | 中 | 「召回 25 条」+ 25 张 MiniCard 入场 4707→**4685**（与流程轨切「重排」同拍），灰梯形「重排」4687 slideUp 占位；**4707**（粗召回几十条）改为卡片 0.5 帧错峰波浪脉冲（抬 6px、放大 12%、微紫）+ 标签 1.06 脉冲；**4734** 梯形灰→紫 11 帧（`mixStops(k)`）+ glitch 序列硬切换字「重排」→「Cross-Encoder 重排」（不做叠影交叉淡化）。配合 G5 SC25 4664 起淡出，组界为「淡出 → 8 帧呼吸 → 4685 入场」 | 4677 / 4686 / 4695 / 4707 / 4712 / 4720 / 4736 / 4740 |
| SC27 5001–5017 TagBlock 未到位即离场 | 中 | 「贵」「准」`scaleIn` 21→**8 帧**（fadeIn 3），绿勾 9→**6 帧**；5014 全部到位并保持到 5017；去掉末 2 帧整镜淡出，5017→5018 **硬切**（SC28 5022 起淡入，中间 4 帧仅星空，与 SC26→SC27 一致） | 5003 / 5010 / 5014 / 5017 |
| SC26 4899–4910 落卡盖住「Cross-Encoder」 | 低 | 25 卡改画在梯形层**之后**：穿层时被梯形遮住、从下缘重新出现（3 张已变紫），字样不再被盖；配对卡 / 双箭头 / 相关分 改为 **4882 起 8 帧淡尽**（原 4892 起 12 帧），4890 落卡前清场 | 4886 / 4900 / 4905 / 4910 |
| SC30 5645「RAG 的价值」贴近字幕带 | 低 | 大字 cy 585→**561**（下缘 ≈581，距字幕带 56px）；Pill 508→486、来源卡 415→405、折线拐点 388→383 随之上移 | 5640 |
| SC29→SC30 5564\|5565 | 中（G0） | 未改，答复卡位置/内容连续；流程轨一帧消失由主会话在 G0 处理 | — |
| 半角 ? → 全角 ？ | 低 | **无需修**：G6 问句源码已是全角「？」（SC26 配对卡、SC29 Prompt 卡） | — |
- 未修：无。SC27「准」块尺寸低项与中项同源，已随 8 帧缩放到位。

## 闪烁整改（用户裁定 2026-09-06「不要给每段出现的字都加闪烁，只给重点加」· RULES §8）
- 改前 **12 处 `GlitchIn` + 2 处 `glitchOpacity`**（SC26 梯形换字、SC29 角标 `citeOp`）= 14 处闪烁；改后 **2 处 `GlitchIn` + 1 处 `glitchOpacity`** = 3 处，其余全部换 `SoftIn`（ui.tsx，8 帧淡入 + 10px 上浮），入场帧 f0 一律不变。
- 保留（白名单）：**SC26「Cross-Encoder 重排」梯形换字**（4734，glitchOpacity 序列硬切）、**SC28「Lost in the Middle」标题**（5154）、**SC30「RAG 的价值」大字**（5633）。SC30 大字原带 `rgbSplit={4}`，§8 规定 rgbSplit/slices 仅限片头/SC08/SC35/SC44，故去掉，改为纯透明度序列 GlitchIn。
- 换掉的：SC26「Rerank」(4685)、「召回 25 条」(4685)、「相关分」框 (4846)、「Top 3」(4914)；SC27「RAG 检索」「推荐系统」Pill (4935/4937)、「≈」(4946)；SC28「中间最差」(5091)、「Liu et al., TACL 2024」(5158)；SC29 角标 [1][2]（5517，`ui.tsx citeOp` 改为 8 帧 easeOut 淡入，签名不变、仍只变透明度不回流）；SC30「可追溯 · 可核查」Pill (5631)。SC30 来源卡本来就是 scaleIn + fadeIn，未动。
- 组内图元 `G6/ui.tsx`：只有 `citeOp` 封装过 glitch（已改软淡入），其余图元不含 GlitchIn，无需加开关；`glitchOpacity` import 已移除。
- `npx tsc --noEmit` 通过。still：`rag/stills/X56/`（tag `x56`，与 G5 共用一个 bundle）：4689/4697、4738/4746（换字 glitch 保留）、4850/4858、4918/4920、4939/4947、4950/4958、5095/5103、5162/5170、5521/5529、5635/5643/5647。
- still 复核结论（Read 逐张看过 21 张）：所有 SoftIn 处 f0+4 约 80% 透明度、f0+12 完全到位，尺寸/坐标与改前一致；4738/4746「Cross-Encoder 重排」换字保留 glitch 硬切；5162/5170「Lost in the Middle」glitch 保留、出处软淡入；5521/5529 角标 [1][2] 软淡入不回流；5635→5643→5647「RAG 的价值」纯透明度 glitch 序列（半透→0.75→全显），无 RGB 错位。无需回修。

## QC v2 补修（主会话复验 C3，2026-09-06）
| 条目 | 严重度 | 处理 | 复核 still |
|---|---|---|---|
| SC27→SC28 5017\|5018 满画面硬切到空场（空 4 帧） | 中 | `index.ts` SC27 `to` 5017→**5021**；SC27 5016–5021 整组 `opacity = 1−(n/5)^1.5`、下摇 `10·(n/5)²` ≤10px，5021 归零后 return null；与 SC28 5018 起入场重叠 4 帧 | 5017 / 5019 / 5021（`rag/stills/X56/`） |
| SC26 4688 灰梯形自 y≈700 slideUp 落在字幕带内 | 低 | slideUp Δ 260→**100**（首帧 y=430、底缘 524 <637），fadeIn 8→**6** 帧 | 4686 / 4688 / 4690 |
