# G8 构建笔记（第 4 章下半：SC38–SC44，S38–S44）

预览合成 `RagG8`；镜头表 `index.ts` → `SHOTS_G8`（7 镜头，相邻首尾相接 6991–8148），`BG_G8 = []`（不覆写星点/雾底）。
第 4 章无流程轨，内容主区 y 110–620；顶部 HUD（y<105，含 Exo 2 副标）由 G0 画，本组不碰。
`npx tsc --noEmit` 通过（最后一次：全部 7 镜头 + shared.tsx）。

## 镜头表

| id | 帧 | 文件 | 内容 / 节拍对齐 | 离场 |
|---|---|---|---|---|
| SC38 多模态 RAG | 6991–7167 | `SC38.tsx` | 6999 「Multimodal RAG」Exo 2 标签 glitch + 三张页面缩略图卡（柱状图/折线图/截图窗口，110×146）2 帧错峰自下滑入；7033 箭头①自根部长出 → 7040 Embedding 紫盒 glitch → 7052 箭头② → 7062 三列向量数字（6 行 × 3，2 帧错峰）；7106 问句 Pill「Q3 营收趋势图在哪？」glitch 于右上，7112 起 16 帧 easeOut 飞到向量列上方，7128 命中：中列数字 11 帧变紫 + 柔光框，折线图卡紫边 + 柔光 + emphasisPulse 1.10 | 7154 起加速下沉（0.6·t²）+ 6.7%/帧淡出 |
| SC39 百万 token | 7168–7301 | `SC39.tsx` | 7176 LLMIcon(230,380) 21 帧缩放 + 灰 Pill「上下文窗口」glitch；7189 Orbitron 96px 计数 0→**1,000,000**（26 帧 easeOutCubic，tabular）+ 「tokens」灰 Exo 2 自下滑入；7260 紫 Pill「RAG」（Audiowide）glitch；7262 橙色「?」Orbitron 180px glitch(rgbSplit 6) + 7274 emphasisPulse 1.15 | 末 2 帧不离场（SC40 硬切） |
| SC40 20 万 token ≈ 500 页 | 7302–7503 | `SC40.tsx`（组件在 `shared.tsx` `RulerPart/WindowPart`） | 7310 灰 Pill「Anthropic, 2024」glitch + 刻度尺 (200→1080, y380) clipPath draw-on 22 帧、刻度 11 根、「0」「1M」；7373 紫竖线在 20% 处 (x376) 自下长出 16 帧 + 6 枚 MiniDoc 2 帧错峰落在 0–200K 段；7377 「200K tokens」glitch；7428 「≈ 500 页」计数 0→500（20 帧）；7455 「上下文窗口」框 (850,140,300×160) glitch；7459 起文档 2 帧错峰 20 帧抛物线飞入框内 3×2 网格；7488 绿勾 draw-on | 不离场：SC41 用同一组件承接（硬切无缝） |
| SC41 搭档不是对手 | 7504–7753 | `SC41.tsx` | 承接 SC40 终态；7512 120 枚小文档方块按「距窗口距离 + rnd 抖动」在 ≈24 帧内堆满窗口并向右堆出画外（x→1420）；7528 绿勾淡出 → 7534 红叉 (96px) 画在窗口上；7593/7601/7609 三枚 ORANGE Pill「权限」「时效」「成本」glitch（窗口左侧竖排）；7641 刻度尺组下沉淡出 15 帧 + 紫「RAG · 找」（放大镜）自左滑入 22 帧；7675 白边「长上下文 · 读」（书页）自右滑入 + 白「+」淡入；7709 两胶囊各向中靠拢 20px（easeInOutPow 2.5，16 帧）、「+」11 帧变紫 + 柔光；7711 紫 Pill「搭档」glitch | 7744–7753 10 帧线性淡出（含末 2 帧） |
| SC42 闭卷→开卷 | 7754–7869 | `SC42.tsx` | **复用 G2 `parts.tsx`**：7762 灰考卷 `ExamPaper`（SC06 同图形，0.9× = 270×342，brightness(.63) 压灰，中心 (400,415)）/ 7764 `BookIcon`（SC06 同尺寸 280×180，中心 (880,415)，7812 右页第 1 行紫高亮擦出）/ 7766 中央 LLMIcon 96 —— 2 帧错峰 21 帧缩放入场；7798 「闭卷」灰 56px glitch → 7802 白箭头 draw-on 18 帧 (480→800, y190) → 7812 「开卷」紫 glitch(rgbSplit 6) + 7822 emphasisPulse 1.12 | 7857 起加速下沉 + 淡出 |
| SC43 手册整理得多好 | 7870–8052 | `SC43.tsx` | 7878 左 (360,395) 灰乱页书（页面歪斜 ±14°、两张外翘散页、翻页姿态 4 态 **3 帧一换**、内容按 step 重随机）/ 7880 右 (920,395) 整齐书（标题条 + 缩进层级线、右侧 5 个索引标签 紫/橙/白/灰/紫、紫高亮行）；7927 灰 Pill「翻得快」glitch + 7931 红叉；7986 紫 Pill「整理得好」glitch + 右书紫 drop-shadow 12 帧亮起 + 7988 emphasisPulse 1.08 + 7990 绿勾 | 8040 起加速下沉 + 淡出 |
| SC44 知识库 = 护城河 | 8053–8148 | `SC44.tsx` | 8061 城堡（主塔 + 双侧塔 + 城齿 + 紫门洞 + 旗）自下滑入 Δ300；8069 紫 Pill「知识库」glitch 于塔顶上方 (640,145)；8071 护城河环带（外 335×78 / 内 245×44 椭圆，evenodd 挖空，紫竖渐变）外/内轮廓 stroke-dash draw-on 20 帧，前半环压在城堡之前、后半环在其后；三条 `16 22` 虚线波纹相位 **2 帧一步**；8077 「知识库 = 护城河」Noto 900 60px scaleX .85 白 + 2.5px 紫描边 glitch(rgbSplit 6) | 末 2 帧不离场（G0 片尾接管淡出） |

## 复用 / 新建图元
- `ui.tsx`：CText / TechText / MonoText / Pill / Box / Svg / LineArrow / Check / Cross / LLMIcon / 调色板 / fadeIn·fadeOut·slideUp·scaleIn·exitAccel·exitFade。
- `common`：GlitchIn / kf 家族 / emphasisPulse / powOutRemain / easeInOutPow / rnd / FONT_TECH·FONT_ORB·FONT_WIDE。
- 本组 `shared.tsx` 新建（可迁入 ui.tsx，见末节）：`PageCard`（页面缩略图卡 3 型）、`EmbedBox`（Embedding 紫盒：紫填充 + 白虚线 r8，参照 STYLE_GUIDE §6.3 Embedding 盒；**G4 SC16 如已有同名盒请对齐样式**）、`MiniDoc`（SVG `<g>` 小文档）、`RulerPart/WindowPart`（SC40→SC41 共用）、`IconPill`（放大镜/书页图标胶囊）、`OpenBook`（SC43 专用：乱页/整齐两变体 + 索引标签 + 翻页姿态）、`Castle`、`Anchor`（定点缩放容器）、`mix`（颜色插值）、`fmtInt`（千分位）。
- **SC42 与 G2 SC06 的复用**：SC42 直接 `import {ExamPaper, BookIcon} from '../G2/parts'`（不改 G2 文件；尺寸/锚点差异在 SC42 内用 Anchor 缩放 + brightness 滤镜适配）。本组早期自实现的 ExamPaper 已删除。

## 关键参数
- 数字与拼写（均见 research/RAG调研.md）：`1,000,000` tokens（§5.4 Gemini / Claude 1M 上下文）；`200K tokens` ≈ `500 页`、`Anthropic, 2024`（§1 / §7.1 #3 Contextual Retrieval 博文）；`Multimodal RAG`（§5.3 多模态 RAG）；`Embedding`（§2）。
- 尺：x 200→1080（880px），20% 刻度 x=376；窗口框 (850,140,300×160)；文档 home `204+27i, 336` → grid `895+90(i%3), 178+58⌊i/3⌋`，飞行 20 帧 easeInOutPow(2.5) + 120·sin(πt) 拱高。
- SC38 标签位置：原分镜「顶部标签」会与 G0 HUD 的 Exo 2 副标（y≈94）叠成三行，改放卡组上方 (325,296)；流程行中心 y 425。
- 节拍：所有关键元素在字幕块起始 −6…+3 帧内出现（问句 Pill 7106 = 7112−6；其余 = 节拍帧或 +2…+4）。

## 测渲计时（协议 §6，当时 bundle `build_dev_g8_5`（全量 ≈820 MB），`--sequence --image-format=jpeg`）
| 区间 | 帧数 | 耗时 |
|---|---|---|
| SC41 7600–7629（最重：溢出方块 + 尺 + 窗口 + 胶囊） | 30 | 23.7 s |
| SC38 7100–7129 | 30 | 24.3 s |
| SC41 7600–7659 | 60 | **18.1 s** |
| SC39 7230–7259（轻） | 30 | 13.4 s |

60 帧比 30 帧还快、轻重镜头同耗时 → 时间被 Chromium 启动/bundle 加载与并发争抢主导（测时 `uptime` load average **88**，八组同时渲染），每帧增量 <0.3 s，无本组性能问题。测渲后已把 SC41 溢出方块从 150×4 节点降到 120×3 节点（≤360 SVG 节点，单帧总 DOM ≈470 < 600）。滤镜：每镜头 ≤3 个 drop-shadow 层（Svg BLOOM / 书本 / 尺），无 blur、无 feConvolveMatrix。

## 自检 still（`rag/stills/G8/`，Read 逐帧看过；`contact_scNN.png` 为 2 列拼图，`contact.py` 为拼图脚本）
- SC38：6991(组界首帧)、7001、7012、7030、7048、7075、7110、7122、7135、7160
- SC39：7169、7180、7195、7205、7218、7262、7268、7285、7301
- SC40：7303、7318、7335、7378、7390、7440、7462、7475、7495、7503
- SC41：7504、7520、7540、7600、7612、7650、7680、7700、7725、7745、7753
- SC42：7755、7766、7778、7800、7815、7835、7860、7869（改用 G2 parts 后复核 7800、7835）
- SC43：7871、7882、7895、7930、7945、7990、8010、8045
- SC44：8054、8064、8075、8082、8095、8120、8148(组界末帧)
- 组界：`boundary_6991_first.png`、`boundary_8148_last.png`
- 发现并已修：① SC38 顶部标签与 HUD 副标叠行 → 移到卡组上方；② SC40 文档叠末枚碰紫竖线 → 左移 8px；③ SC44 「知识库」胶囊底边碰旗尖 → 旗杆缩短 12px；④ SC41 溢出方块节点数 → 120×3。
- 检查项：无内容进入 y<110 / y>620（SC41 溢出方块最高到 y≈100 区且在 x≥850，避开 HUD 胶囊；溢出向右出画）；字幕带 y637–690 无可读内容；英文拼写 Multimodal RAG / Embedding / Anthropic / tokens / RAG 已核对；颜色仅用调色板 + 三色点（mac 窗口配色）。

## 未完成 / 降级
- 无降级。SC39「有人问」节拍（7176）用 LLMIcon + 「上下文窗口」灰胶囊表现，未画问句气泡（避免与 7260 的橙色大问号重复）。
- 磁盘：构建期间磁盘一度只剩 118 MiB（15 个 build_dev bundle ≈11 GB），已通知主会话。按新规则本组 bundle 固定 tag `g8`（`remotion/build_dev_g8`，精简 bundle ≈110 MB），改码后 `rm -rf remotion/build_dev_g8` 再跑 still.sh；已用新版脚本复验 7600/8148 两帧正常。

## 对共用层的建议
1. `EmbedBox`（Embedding 紫盒）建议与 G4 SC16 的同款对齐后提升到 `ui.tsx`；若 G2 `ExamPaper` 能加 `color` prop，SC42 就不必用 brightness 滤镜压灰。
2. `Anchor`（定点缩放容器，自动 `transformOrigin 0 0`）与 `mix`（颜色插值）是各组都会写的胶水，建议进 ui.tsx。
3. `still.sh` 输出目录参数若传相对路径会落到 remotion/ 下（脚本内 cd），建议文档注明必须绝对路径（我第一次就踩了）。

## QC v1 修复（2026-09-06，依据 rag/qc/qc_v1_C4.md §2；bundle tag `g78`，still 见 `rag/stills/F78/`）
只改 `src/rag/shots/G8/**`；`npx tsc --noEmit` 通过。SC44 按主会话裁定保持到 8148 不离场（片尾压黑由 G0 覆盖层做）。

| QC 条目 | 严重度 | 修法 | 核对 still |
|---|---|---|---|
| SC38 7167｜7168 / SC42 7869｜7870 / SC43 8052｜8053 离场末帧仍 50–60% 可见 | 中 ×3 | `shared.exitOut(ex,len)`（与 G7 同式：下摇 1.2·ex² + 6.7%/帧淡出，末 6 帧线性收 0）；三处统一 `exitOut(N−(to−14), 14)`，起点 7153 / 7855 / 8038，末帧恰为 0 | 7165 / 7167（主区 >60 像素 252 ≈ 基线）、7867 / 7869（300）、8050 / 8052（255） |
| SC39→SC40 7301｜7302 硬切落到空帧 | 中 | SC39 末 6 帧 `exitOut(N−7295, 6)`，7301 恰为 0（与 G7 SC35/SC37 同一做法） | 7298 / 7301 / 7302 |
| SC41 7520–7753「上下文窗口」标签被溢出方块 + 红叉遮死 | 中 | `WindowPart` 标签移到框外左上 (cx 916, cy 120)，20→24px + 黑影；顶部冒出的 10 枚方块 x 范围改为 ≥1000（原 870–1110），与标签 (x 856–976) 错开；SC40 同组件同步受益 | 7600 / 7649 |
| SC43 7870–7989 (1032,555) 绿点常驻 | 中 | Check / Cross 在 p≤0 时不渲染（SC41 红叉同样加了守卫） | 7900（该区域 G 通道 max 18） |
| SC38 7001–7010 卡片自下滑入穿字幕带 | 中 | slideUp Δ 300→100 + fadeIn 10→8 帧（n=0 时卡底 y≈608 / 图注 ≈624，均在 637 之上） | 7004 / 7008 |
| SC40 7302–7309 刻度尺左端封口提前 8 帧孤立可见 | 低 | clipPath 宽度在 p≤0 时为 0（原 8px 露出端帽），端帽随尺身 7310 一起揭示 | 7302 / 7306 / 7311 / 7318 |
| SC40「Anthropic, 2024」约 16px | 低 | 23→25px，胶囊 214→232 宽 | 7318 |
| SC38 向量数字 / 图注小 | 低 | mono 20→22px（x −30）；「柱状图 / 折线图 / 截图」22→24px | 7075 |
| SC41 7644–7658「RAG · 找」压在淡出的尺子上 | 低 | 尺组淡出起点 7641→7633（提前 8 帧） | 7649 |
| SC44 8061–8072 城堡 Δ300 穿字幕带 | 低 | Δ 300→170（起点城堡底 y≈590）；末帧不离场保持不变 | 8066 |

**未修**：无（G8 §2 全部条目已处理；片尾 8148｜8149 属 G0）。

## 闪烁整改（2026-09-06，用户裁定「不要给每段出现的字都加闪烁，只给重点加」，依据 AGENT_RAG_BUILD_RULES §8）
只改 `src/rag/shots/G8/**`；非重点入场一律换 `ui.tsx` 的 `SoftIn`（8 帧淡入 + 10px 上浮，容器同为 absolute inset:0，几何/入场帧 f0 均不变）。`npx tsc --noEmit`：G8 无错误（当时仅 G1/SC05 有他组在改的 `GlitchIn` 未定义报错）。bundle tag `x78`，still 见 `rag/stills/X78/`（每处 f0+4 / f0+12 各一张 + 拼版 `sheet_G8_*.png`）。

| 计数 | GlitchIn 代码处 | 运行时实例 |
|---|---|---|
| 改前 | 21（含 shared.tsx 4 处） | 23（SC41 橙 Pill 循环 ×3） |
| 改后 | 3 | 3 |

**保留（白名单）**：SC38「Multimodal RAG」（6999）、SC42「开卷」（7812；按 §8「rgbSplit 只允许片头/SC08/SC35/SC44」去掉了原 rgbSplit 6，改为普通 12 帧 GlitchIn）、SC44「知识库 = 护城河」大字（8077，保留 rgbSplit 6）。
> 待主会话确认：白名单写的是「闭卷 → 开卷」大字，但 §8 又规定每镜头最多 1 处 glitch；我按「重点词 = 紫色开卷」处理，灰色「闭卷」改 SoftIn。若希望「闭卷」也闪，只需把 SC42.tsx 7798 那处 SoftIn 换回 GlitchIn。

**组内图元开关**：`shared.tsx` 新增 `EntryIn`（`glitch?: boolean`，默认 false → SoftIn；true → GlitchIn），`RulerPart` / `WindowPart` 各加 `glitch?: boolean` 透传（默认 false）。SC40/SC41 不在白名单，调用处不开。

**换成 SoftIn**：
- shared.tsx：「Anthropic, 2024」框（7310）/「200K tokens」（7377）/「≈ N 页」（7428）/ 上下文窗口框 + 标题（7455）—— SC40、SC41 共用
- SC38 「Embedding」盒（7040）/ 问句 Pill「Q3 营收趋势图在哪？」（7106）
- SC39 「上下文窗口」灰 Pill（7176）/ 计数大字（7189）/「RAG」紫 Pill（7260）/ 橙色大问号（7262，原 rgbSplit 6 一并去掉，脉冲不变）
- SC41 「权限 / 时效 / 成本」橙 Pill（7593 起 8 帧错峰）/「搭档」Pill（7711）
- SC42 「闭卷」灰大字（7798）
- SC43 「翻得快」（7927）/「整理得好」（7986）
- SC44 「知识库」紫 Pill（8069）

核对 still：7044/7052、7110/7118、7180/7188、7193/7201、7264/7272、7314/7322、7381/7389、7432/7440、7459/7467、7597/7605、7715/7723、7802/7810、7816/7824、7931/7939、7990/7998、8073/8081 —— f0+4 均为半透明中段、f0+12 完全到位，位置与尺寸与改前一致。
