# 《RAG 与知识库》原创 MG 科普片 · 交付说明（v4，2026-09-06）

## 成片
- `renders/rag_v4.mp4` — 1280×720 @30fps，8246 帧 = 4 分 35 秒，H.264 crf16 + AAC 配音；`renders/sheet_v4.html` 每 60 帧缩略总表（v1–v3 为中间版本，保留供对比）（配合 `fin_frames/` 逐帧 jpg 可点看任意帧）。
- 配音：edge-tts `zh-CN-YunxiNeural`（+8%，≈6 字/秒），逐句合成后按词边界切成字幕块；音频 `remotion/public/assets/rag/audio.wav`。**换音色/语速只需改 `script/tts_build.py` 的 VOICE/RATE 并重跑，但所有镜头帧号会随之变化**（构建组代码里帧号硬编码），需要重新对位。
- 四章：为什么要RAG（f86）/ 知识库构建（f1534）/ 检索与生成（f3716）/ 评估与进阶（f5750）；44 句解说、1490 字，见 `script/narration.txt` 与 `script/timeline.md`。

## 与推荐系统片"同技术同风格"的落实
- 同一个 Remotion 工程（`remotion/src/rag/`），直接 import 推荐系统片的雾底 Fog、星点 StarField、12 帧 GlitchIn、缓动库、字幕样式（44px Noto 700 白字黑边）、进度条模型（半透明条体 + 4 章名 + 分隔线）；顶部 HUD 胶囊沿用 (533,28,216,51)；调色板/字体（Noto Sans SC 900 压窄、Exo 2 Italic 紫色英文技术词、Audiowide 大字、Times 公式、Orbitron 数字）与动效词汇（自下滑入 (1−n/22)^2.5、21 帧缩放入场、箭头自根部长出、灰→紫 11 帧变色、emphasisPulse 1.11、幂缓入加速离场）全部按 `STYLE_GUIDE.md` 执行。
- 新增本片专用：`ui.tsx` 图元库（DocIcon/DBIcon/ChunkCard/LLMIcon/TagBlock/LineArrow/Check/Cross/Counter…）、五步流程轨（第 2、3 章）、章节卡、片尾压黑。

## 内容与事实
- 调研底稿 `research/RAG调研.md`（约 7900 字、105 个来源 URL、9 处【待核】）。画面上出现的全部数字/英文均可在调研 §7 找到出处：Lewis et al. NeurIPS 2020；Anthropic Contextual Retrieval 2024（含 BM25 时检索失败率 −49%）；Lost in the Middle（Liu et al., TACL 2024）；RRF k=60（SIGIR 2009）；200K tokens ≈ 500 页（Anthropic）；GraphRAG（Microsoft Research 2024）；RAGAS 四指标。
- 全片示例统一"差旅报销"语境：问句「差旅报销的上限是多少？」、幻觉答复「每天 1000 元，全国统一」、正确答复「住宿上限每天 600 元[1]，一线城市 800 元[2]」、来源「员工手册 p.12」「差旅补充规定 2025」；SC24 的型号 X-2000B 与相似度 0.83/0.81/0.79、SC34 的「每天 500 元」为示意值。

## 质检
- v1 由 4 个 QC agent 按 `AGENT_RAG_QC_RULES.md` 逐章检查（`qc/qc_v1_C1–C4.md`）：高 1 / 中 23 / 低 42；根因集中在"硬切落空帧"、"入场穿字幕带"、"离场未归零"、"覆盖层出场太短"。全部由 4 个修复 agent + 主会话修复（各组 BUILD_NOTES 末尾「QC v1 修复」），v2 复验见 `qc/qc_v2_recheck_C12.md`、`qc/qc_v2_recheck_C34.md`（66 条中 62 已修、3 部分、1 有意保留；新增 6 条已在 v3 修掉）。
- **v3/v4 变更（用户裁定"闪烁只给重点"）**：全片 GlitchIn 由 102 处（含循环内多元素）降到 21 处，每镜头 ≤1 处、仅用于该镜头的重点词（白名单见 `AGENT_RAG_BUILD_RULES.md` §8：片头/章节卡、三个短板标签、开卷考试、RAG、Embedding、HNSW、语义检索、混合检索、Cross-Encoder 重排、Lost in the Middle、RAG 的价值、RAGAS、RAG（进化）、GraphRAG、Agentic RAG、Multimodal RAG、开卷、知识库 = 护城河）；其余文字/标签/HUD 换词一律 8 帧淡入上浮（`ui.tsx` `SoftIn`）；带 RGB 错位/切片的重口味 glitch 只留片头、章节卡标题、SC08、SC35、SC44。v3 终检（`qc/qc_v3_final.md`）：全片 12 帧闪烁模板扫描命中 23 处，与白名单 23 项一一对应，白名单外 0 处；v4 仅把淡入首帧从 0 提到 ≈25% 可见，消掉 HUD 换词的 1 帧空档。
- 已知保留项（有意为之）：Embedding 维度用「×1024 维」示意；SC18 HNSW 路径 22 跳示意"几十次"；SC37 智能体环为三节点 + 出口分支；SC33 Context Precision/Recall 灰态（旁白未提）。

## 目录
`research/` 调研 · `STYLE_GUIDE.md` 风格指南 + `style_ref/` 参考帧 · `script/` 解说词/配音/时间轴/工具 · `分镜表.md` · `AGENT_RAG_BUILD_RULES.md` / `AGENT_RAG_QC_RULES.md` · `stills/` 各组自检帧 · `qc/` 质检报告 · `renders/` 成片（v4 为交付版） · `remotion/src/rag/` 源码（`shots/G0` 覆盖层，`G1–G8` 八个构建组，各组 `BUILD_NOTES.md` 记录参数与偏离）。
